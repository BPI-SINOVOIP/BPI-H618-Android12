# Script to start "wm" on the device, which has a very rudimentary
# shell.
#
base=/system
export CLASSPATH=$base/framework/displayoutput-test.jar
exec app_process $base/bin com.android.commands.dispctl.dispdclient "$@"
